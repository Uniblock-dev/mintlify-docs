---
title: 'getEvents'
api: 'POST https://api.uniblock.dev/uni/v1/json-rpc'
playground: 'none'
---
### Parameters

Event filter and pagination options.

<ParamField body='params.startLedger' type='number'>
  Inclusive starting ledger sequence to search from.
</ParamField>
<ParamField body='params.endLedger' type='number'>
  Exclusive ending ledger sequence to stop at.
</ParamField>
<ParamField body='params.contractIds' type='string[]'>
  Optional list of contract IDs to filter on.
</ParamField>
<ParamField body='params.topics' type='string[]'>
  Optional list of topic filters. Provider-specific matching may apply.
</ParamField>
<ParamField body='params.pagination' type='object'>
  Pagination controls.
</ParamField>

### Returns

<ResponseField name='result' type='object'>
  Event page containing matched events and a paging cursor.
  <Expandable title='properties'>
    <ResponseField name='events' type='array'>
      Matched events.
      <Expandable title='items'>
        <ResponseField name='items[]' type='object'>
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name='cursor' type='string'>
      Cursor to fetch the next page, or null if complete.
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash Curl
curl --request POST \
  --url https://api.uniblock.dev/uni/v1/json-rpc?chainId=stellar-soroban \
  --header 'content-type: application/json' \
  --data '{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "getEvents",
    "params": {
      "startLedger": "LEDGER_SEQUENCE",
      "pagination": {
        "limit": 100
      }
    }
  }'
```

</RequestExample>
<ResponseExample>

```json Success
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {}
}
```

</ResponseExample>
